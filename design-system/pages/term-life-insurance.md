# Page override: Term Life hub (`/term-life-insurance/`)

Inherits `design-system/MASTER.md`. Only deviations are listed.

- **CTA weighting (spec 09):** form-weighted. The three-step quote form is a `.panel` in the hero's
  right column, above the fold at 1024. Phone lives in the sticky header only, plus the form
  success state.
- **Italic line:** under the H1.
- **Term-length selector:** choice chips at 64px, one `.card` below swapping its panel.
- **Rate table:** `[PLACEHOLDER]` cells, dated source line, row-level "Quote this" buttons that
  prefill the hero form and jump to the first empty step.
- **Underwriting timeline:** sticky left column with the photograph, five soft-dotted steps right.
- **Final form CTA** repeats the same three-step form in a `.panel`.
