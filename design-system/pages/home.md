# Page override: Home (/)
Inherits design-system/MASTER.md. Only deviations are listed.

- Triage, not pitch. The hero is the only kinetic headline on the site: two authored lines, the
  key phrase "depend" carries the one `.u-key` orange underline.
- Hero grid 7/5 with the trust rail spanning both columns beneath; on phones the image is ordered
  after the rail so licensing and the carrier strip stay inside the first screen.
- Three product paths are an asymmetric 5/4/3 row with column rules, each carrying its silo's CTA
  weighting (term form CTA with the magnetic pull; whole form + ghost phone; final `btn-call`
  with hours). The trust rail carries no byline on this page (home has no `byline()` block; the
  independence section links `/about/agents/` once).
- "How we work" is the sticky-stack with the heading column pinned beside it.
- The one navy section is independence + commission disclosure, with the reviews slot still
  hidden and marked. No photograph behind it.
- Comparison table keeps its `tfoot` CTA row; `data-cta-location` values unchanged from v1.
- Layout families in order: split hero + rail / asymmetric card row / split + panel / stack /
  navy split / table / split accordion / split CTA.
- Eyebrow budget 3, used 3 (hero, how we work, why independent).
