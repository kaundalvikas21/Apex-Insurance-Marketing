# Page override: Home (`/`)

Inherits `design-system/MASTER.md`. Only deviations are listed.

- **Purpose:** brand and triage. Links to the three hubs, never to spokes.
- **Hero:** split, copy left (H1, the page's one italic line, lead), duotone photograph right. This
  is the only duotone image on the site. The one eager image on the page.
- **Product paths:** three soft cards (`.card.card-hover`), staggered reveal. Term carries a gold
  form CTA; whole life a gold CTA plus a ghost phone CTA; final expense a forest phone CTA. They are
  deliberately not visually identical.
- **Trust strip** sits directly beneath the paths, inside the same viewport at 1440.
- **Triage widget:** `.panel`, no email wall, routes to a hub section anchor.
- **The forest band** is the independence and commission section. No photograph behind it. The
  agent card inside it is a raised `forest-700` surface, not glass.
- **Final CTA:** two `.card`s split by intent, form left, phone right.
- Layout families in order: split hero, card row, strip, two-column widget, step trio, dark band
  with aside, table, two-column FAQ, card pair. No family repeats adjacently.
