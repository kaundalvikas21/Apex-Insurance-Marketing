{
  "name": "apex-insurance-marketing",
  "version": "1.0.0",
  "private": true,
  "description": "Apex Insurance Marketing, LLC — static marketing site (home, three product hubs, contact).",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/site.css --minify",
    "dev:css": "tailwindcss -i ./src/input.css -o ./assets/site.css --watch",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
